spring.application.name=atomdrops

# Database
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.uurqvmqgycpbigfsrksk
spring.datasource.password=\#qQ33847099
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA — schema managed manually via migration SQL; validate on startup
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Disable schema.sql init — schema managed via Supabase SQL Editor
spring.sql.init.mode=never

# Session security
server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.http-only=true
server.servlet.session.timeout=7d

# Keep session open for lazy loading during serialization
spring.jpa.open-in-view=true

# Mail (SMTP) — configure with real credentials in production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.base-url=http://localhost:8080
